#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    short a, b, c, d, e, f;    
    cin >> a >> b >> c >> d >> e >> f;
    
    cout << fixed << setprecision(9) << (a + b + c + d + e + f) / 6.0 << endl;
    return 0;
}